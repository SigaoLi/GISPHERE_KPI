# GISource 团队绩效管理面板

基于 Streamlit 构建的团队绩效管理系统，用于可视化展示 GISource 团队的工作绩效数据。

## 功能特点

- 📊 **关键指标**: 入库总数、活跃成员数、平均提前天数、最近7天新增
- 🏆 **成员贡献排行**: 直观展示每位成员的贡献量
- 📈 **每日工作趋势**: 追踪团队和个人的每日工作情况
- ⏳ **信息时效性分析**: 分析信息发布的提前期（支持 "Soon" 按 30 天计算）
- 🌍 **国家分布**: 展示信息来源的地理分布
- 💼 **职位类型分布**: 统计不同类型的职位信息

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置文件

确保以下文件存在：

- **credentials.json**: Google API 密钥（用于访问 Google Sheets）
- **sql_credentials.txt**: 数据库连接配置

sql_credentials.txt 格式：
```ini
[MySQL]
host = your_host
port = 3306
user = your_username
password = your_password
database = TEST
```

### 3. 运行

```bash
streamlit run Dashboard.py
```

系统会自动打开浏览器访问 http://localhost:8501

## 数据说明

### 数据源

1. **Google Sheet ("Filled" 表)**
   - Verifier: 核验人
   - Source: 信息来源 URL
   - Deadline: 申请截止日期（支持日期格式或 "Soon"）
   - Direction: 研究方向
   - University_CN: 学校中文名

2. **MySQL (GISource 表)**
   - Date: 入库时间
   - Description: HTML 格式描述（包含 URL 和 Deadline）
   - Country_CN: 国家中文名
   - Job_CN: 职位类型

### 匹配逻辑

使用复合键（URL + Deadline）进行数据匹配：
- 从 MySQL Description 字段提取 URL 和 Deadline
- 从 Google Sheet 读取 Source 和 Deadline
- 通过复合键进行 INNER JOIN

### 时效性计算

- **Lead Time** = Deadline - 入库时间
- **"Soon"** 按入库日期 + 30 天计算
- 数值越大表示提前量越多，时效性越好

## 使用说明

### 侧边栏

- **时间范围**: 7天 / 14天 / 30天 / 60天 / 90天 / 180天 / 365天 / 全部
- **刷新数据**: 重新从数据源读取最新数据

### 数据筛选规则

系统自动过滤：
- Verifier 为空的记录
- Verifier 为 "LLM" 的记录
- IS_Deleted = 1 的记录

## 常见问题

**Q: 首次运行需要授权？**  
A: 浏览器会打开 Google 授权页面，授权后自动生成 token.pickle 文件。

**Q: Token 过期？**  
A: 删除 token.pickle 文件，重新运行程序重新授权。

**Q: 修改端口？**  
A: 使用 `streamlit run Dashboard.py --server.port 8080`

**Q: 平均提前天数显示 0？**  
A: 确保数据中有有效的 Deadline（日期格式或 "Soon"）。

## 技术栈

- Streamlit >= 1.28.0
- Pandas >= 2.0.0
- Plotly >= 5.17.0
- MySQL Connector Python >= 8.0.33
- Google Sheets API v4

## 维护

- 检查更新: `pip list --outdated`
- 升级依赖: `pip install --upgrade -r requirements.txt`
- 备份敏感文件: credentials.json, sql_credentials.txt

---

**版本**: v1.0  
**最后更新**: 2025-11-26  
**团队**: GISource
